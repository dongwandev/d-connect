# 회의 — 2026-05-27 (개발 착수 전 결정사항)

| 항목         | 내용                                |
|--------------|-------------------------------------|
| **일시**     | 2026-05-27 ?? : ?? (TBD)            |
| **장소**     | TBD                                 |
| **참석**     | TBD                                 |
| **진행자**   | TBD                                 |
| **기록자**   | TBD                                 |
| **소요 예상** | 90~120분                            |
| **목적**     | 5/28 개발 착수 전, 아키텍처·DB·API·역할 분담 확정 |

---

## 사전 일독 (회의 시작 전까지)

각 5분 이내 분량.

- 📋 [PRD](../PRD.md) — 제품 요구사항 (특히 §3 핵심 기능, §5 비기능 요구사항)
- 🏛️ [ADR-0001 — 기술 스택](../adr/0001-tech-stack.md)
- 🏛️ [ADR-0002 — LLM Provider](../adr/0002-llm-provider.md)
- 🤝 [GITHUB 협업 가이드](../GITHUB_GUIDE.md) — 특히 [위험 작업 기준](../GITHUB_GUIDE.md#위험-작업-기준)
- 🤖 [CLAUDE.md](../../CLAUDE.md) — AI 도구 사용 컨텍스트

---

## 회의 목표 (Definition of Done)

회의가 끝나면 다음이 결정·공유되어야 한다.

- [ ] DB 핵심 엔티티 6종과 관계 합의
- [ ] 주요 API 엔드포인트 8~10개 계약 초안
- [ ] 컴포넌트 레이어 구조 합의
- [ ] 팀 역할 분담 확정
- [ ] 첫 작업 Issue 5~10개 발행
- [ ] 솔로 정책 → `required_approving_review_count: 1` 복원 (collaborator 추가 직후)

---

## 운영 규칙 (회의 중)

- **항목당 5~15분** cap. 결정 안 나면 "다음 회의로" 마크하고 진행.
- 결정사항은 **이 문서에 즉시 기록**한다. 메모리에 남기지 말 것.
- 위험 작업(production dep / schema / 인증 등)은 회의 후 본인이 PR로 정리.

---

# 🔴 필수 결정 (60~70분)

## 1. 아키텍처 — 레이어 구조 (15분)

### 1.1 컴포넌트 계층

**질문:** 어디까지 분리할지?

**후보:**
- **A. 4-layer** — `Page → Route Handler → Service → Repository (Prisma)`
- **B. 3-layer** — `Page → Route Handler → DB/AI 직접 호출` (단순)
- **C. 그 외** — 메모에 기록

**결정:** `[ ] A` `[ ] B` `[ ] 그 외`

**메모:**


### 1.2 폴더 구조 (`src/` 하위)

**후보:**
- **A. 기능 분리** — `src/app/` / `src/server/` (service, repository) / `src/lib/` (util) / `src/components/`
- **B. 도메인 분리** — `src/app/` / `src/domain/{company,sdg,content}/` (각 도메인 안에 service/repo/types)

**결정:** `[ ] A` `[ ] B` `[ ] 그 외`

**메모:**


### 1.3 AI 호출 흐름 모듈 배치

**질문:** prompt 빌드 → LLM 호출 → 응답 검증(zod) → mock fallback. 한 모듈로 묶을지, 분리할지?

**후보:**
- **A. 단일 모듈** — `src/server/ai/<feature>.ts` 하나에 모두
- **B. 분리** — `src/server/ai/{prompts,client,schemas,mock}.ts`

**결정:** `[ ] A` `[ ] B`

**메모:**


### 1.4 Prisma Client 인스턴스화

**질문:** Next.js dev HMR로 인한 다중 인스턴스 문제 어떻게?

**후보:**
- **A. 글로벌 싱글톤** — `src/server/db.ts`에 `globalThis.prisma` 패턴 (표준)
- **B. 모듈 그대로** — 일단 import해서 쓰고, 문제 생기면 처리

**결정:** `[ ] A` `[ ] B`

**메모:**


### 1.5 에러 처리 정책

**후보:**
- **A. throw + Route Handler에서 catch → JSON envelope** — `{ error: { code, message } }`
- **B. Result 패턴** — 함수가 `{ ok: true, data } | { ok: false, error }` 반환

**결정:** `[ ] A` `[ ] B`

**메모:**


---

## 2. DB 스키마 — 핵심 엔티티와 관계 (25분)

### 2.1 후보 엔티티 6종

| 엔티티              | 역할                                              | 채택? |
|---------------------|---------------------------------------------------|-------|
| `Company`           | 기업 기본 정보 (이름, 업종, 지역, 제품·서비스)   | [ ]   |
| `Activity`          | 지역사회 기여 활동 (Company 1:N)                  | [ ]   |
| `SdgAnalysis`       | SDGs 추천 결과 1세트 (Company 1:N, 시점별)        | [ ]   |
| `SdgMatch`          | 분석 안의 개별 SDG 매칭 (SdgAnalysis 1:N)         | [ ]   |
| `SocialFunction`    | 사회적 기능 분류 (SdgAnalysis 1:N)                | [ ]   |
| `GeneratedContent`  | 생성된 콘텐츠 초안 (SdgAnalysis 1:N, 유형별)      | [ ]   |

**제외/병합 여부:** _(메모)_


### 2.2 ID 전략

**후보:** `[ ] cuid()` `[ ] uuid()` `[ ] Int autoincrement`

**메모:**


### 2.3 시간 추적

`createdAt` / `updatedAt` 전 엔티티 적용: `[ ] Yes` `[ ] No`

**메모:**


### 2.4 Soft delete

도입: `[ ] Yes (deletedAt)` `[ ] No (hard delete)`

**메모:**


### 2.5 enum 처리 (SQLite 제약)

SQLite은 Prisma enum이 string으로 저장됨. 유형:

- `ContentType` — `SNS_POST` / `CARD_NEWS` / `SHORT_VIDEO_SCRIPT` / `CAMPAIGN_SLOGAN`
- `SdgGoal` — `SDG_8` / `SDG_11` / `SDG_12` / `SDG_17` (MVP 제한)
- `SocialCategory` — `EMPLOYMENT` / `ENVIRONMENT` / `LOCAL_ECONOMY` / `COMMUNITY` / `COOPERATION`

**결정:** `[ ] Prisma enum 사용` `[ ] string + 코드 상수`

**메모:**


### 2.6 `Activity`의 정형화 정도

**질문:** 활동을 자유 텍스트로 받을지, 카테고리 + 설명으로 받을지?

**후보:**
- **A. 자유 텍스트만** — `description: String`
- **B. 카테고리 + 설명** — `category: SocialCategory, description: String`
- **C. 카테고리(여러개) + 설명** — M:N for category

**결정:** `[ ] A` `[ ] B` `[ ] C`

**메모:**


### 2.7 산출물

- [ ] 회의 후 [DB_SCHEMA.md](../DB_SCHEMA.md) PR 발행 담당자 지정
- [ ] `prisma/schema.prisma`에 모델 추가 + 첫 마이그레이션 PR 담당자

**담당:**


---

## 3. API — 엔드포인트 계약 (20분)

### 3.1 후보 엔드포인트

| Method | Path                                       | 용도                                | 채택? |
|--------|--------------------------------------------|-------------------------------------|-------|
| POST   | `/api/companies`                           | 기업 + 활동 입력                    | [ ]   |
| GET    | `/api/companies`                           | 기업 목록 조회                      | [ ]   |
| GET    | `/api/companies/[id]`                      | 기업 상세 조회                      | [ ]   |
| POST   | `/api/companies/[id]/sdg-analysis`         | SDGs 분석 요청 (AI 호출)            | [ ]   |
| GET    | `/api/companies/[id]/sdg-analysis`         | 분석 이력 조회                      | [ ]   |
| POST   | `/api/sdg-analysis/[id]/content`           | 콘텐츠 생성 (AI 호출)               | [ ]   |
| GET    | `/api/sdg-analysis/[id]/contents`          | 생성 콘텐츠 목록                    | [ ]   |
| PATCH  | `/api/contents/[id]`                       | 사용자가 초안 수정                  | [ ]   |
| DELETE | `/api/contents/[id]`                       | 초안 삭제 (필요?)                   | [ ]   |

**추가/제거:** _(메모)_


### 3.2 응답 envelope

**후보:**
- **A. 단순** — 성공 시 그냥 데이터, 실패 시 `{ error: { ... } }`
- **B. 표준 envelope** — `{ data?, error? }` 항상 통일

**결정:** `[ ] A` `[ ] B`

**메모:**


### 3.3 에러 응답 표준

**후보:**

```json
{ "error": { "code": "VALIDATION_ERROR", "message": "...", "details": {} } }
```

**code 종류 (1차):** `VALIDATION_ERROR` / `NOT_FOUND` / `AI_FAILED_FALLBACK_USED` / `INTERNAL_ERROR`

**채택:** `[ ] 위 그대로` `[ ] 다음과 같이 수정`

**메모:**


### 3.4 AI 호출 응답 패턴

**질문:** SDGs 분석 / 콘텐츠 생성은 5~30초 걸릴 수 있다. 응답을 어떻게?

**후보:**
- **A. 동기** — 요청-응답 한 번에. 타임아웃 30초.
- **B. 비동기 + 폴링** — `202 Accepted` + 결과 ID 반환 → 클라이언트가 `GET`으로 폴링
- **C. 스트리밍** — SSE/streaming response로 점진 표시

**결정:** `[ ] A` `[ ] B` `[ ] C`

**메모:**


### 3.5 Validation 위치

**후보:**
- **A. Route Handler 진입 시 zod parse** — 실패 시 즉시 400
- **B. Service 레이어에서 검증** — Route Handler는 얇게

**결정:** `[ ] A` `[ ] B`

**메모:**


### 3.6 산출물

- [ ] 회의 후 [API.md](../API.md) PR 발행 담당자 지정

**담당:**


---

# 🟡 중요 결정 (30~40분)

## 4. UI / 화면 구성 (15분)

### 4.1 페이지 라우트

**후보:**

```
/                                # 입력 시작 (랜딩 페이지 없음 — PRD 결정)
/companies/[id]                  # 기업 상세
/companies/[id]/analysis         # SDGs 분석 결과
/companies/[id]/content          # 콘텐츠 생성·관리
/companies                       # 기업 목록 (필요?)
```

**채택:** _(메모)_


### 4.2 shadcn/ui 도입?

**후보:**
- **A. Tailwind만** — 컴포넌트 직접 작성. 빠르지만 일관성 도전.
- **B. shadcn/ui** — 일관된 UI 키트. 약간의 학습 비용.

**결정:** `[ ] A` `[ ] B`

**메모:**


### 4.3 폼 라이브러리

**후보:**
- **A. `react-hook-form` + zod** — 검증 일관성 높음
- **B. 단순 `useState`** — 폼이 적으면 충분

**결정:** `[ ] A` `[ ] B`

**메모:**


### 4.4 디자인 톤

공공기관/지자체 신뢰감 톤. 색상 팔레트 1차 후보:

**메모:**


### 4.5 반응형

PRD §5.5: 데스크탑 우선. 모바일 반응형은 선택.

**확정:** `[ ] 데스크탑만` `[ ] 데스크탑 + 모바일 반응형`


---

## 5. AI 통합 — 구체화 (15분)

### 5.1 모델 ID 확정

Anthropic 콘솔에서 현재 유효한 ID로 갱신:

- `LLM_MODEL_DEFAULT` = ___________
- `LLM_MODEL_FAST` = ___________

(`.env.example` 갱신 PR 필요)

### 5.2 SDGs 매칭 키워드 사전 1차 초안

SDG 8 / 11 / 12 / 17 각각 ~10개 키워드씩.

**담당:**

**메모:**


### 5.3 시스템 프롬프트 초안

공공홍보 톤 가드 + few-shot 예시 1~2개.

**담당:**

**메모:**


### 5.4 Tool Use JSON 스키마

SDGs 추천 결과:

```ts
{ matches: [{ sdg: 'SDG_8', score: 0~100, keywords: string[], rationale: string }] }
```

콘텐츠 생성 결과:

```ts
{ body: string, hashtags: string[], imagePrompt: string }
```

**채택:** `[ ] 위 그대로` `[ ] 다음과 같이 수정`

**메모:**


### 5.5 mock 응답 데이터셋

시연용 예시 기업 2~3개에 대한 모킹 응답 미리 준비.

**담당:**


---

## 6. 테스트 전략 (10분)

### 6.1 단위 테스트 도구

**확정:** `[ ] Vitest` `[ ] Jest` `[ ] 그 외`

### 6.2 통합 테스트 범위

**후보:**
- Route Handler까지 — AI mock 사용
- Service까지 — Prisma는 in-memory SQLite

**결정:**

**메모:**


### 6.3 E2E

**후보:** `[ ] Playwright 도입` `[ ] MVP 범위에서 제외`

**메모:**


### 6.4 커버리지 목표

부트캠프 일정 압축이라 빡빡한 목표 금지 권장.

**목표:** _(예: lint/typecheck/build 통과 + 핵심 서비스 함수 단위 테스트만)_


---

# 🟢 운영/협업 결정 (15분)

## 7. 팀 구성

### 7.1 GitHub collaborator 초대

팀원 GitHub 아이디 목록 (회의 시작 전에 받기):

| 이름 | GitHub 아이디 | 권한 |
|------|---------------|------|
|      |               | push |
|      |               | push |
|      |               | push |

회의 종료 직후 다음 명령 실행:

```bash
gh api repos/dongwandev/d-connect/collaborators/<username> -X PUT -f permission=push
```

### 7.2 역할 분담

| 역할              | 담당 |
|-------------------|------|
| FE (페이지·컴포넌트) |      |
| BE (Route/Service/DB) |    |
| AI 통합 (프롬프트·스키마) |  |
| 디자인 / UX        |      |
| 문서 / 운영        |      |

> 부트캠프 규모상 한 사람이 여러 역할 겸임. 명목적 책임자만 분명히.

### 7.3 솔로 정책 → 정식 정책 복원

collaborator 추가 직후 실행:

```bash
gh api \
  --method PATCH \
  repos/dongwandev/d-connect/branches/main/protection/required_pull_request_reviews \
  -F required_approving_review_count=1
```

**담당:**


---

## 8. 협업 인프라

### 8.1 Labels 생성

가이드 §3에 정의된 9종. 회의 후 일괄 생성:

```bash
# 예시 (실제 실행은 회의 후)
gh label create "type: bug"      --color B60205
gh label create "type: feature"  --color 0E8A16
gh label create "type: refactor" --color D4C5F9
gh label create "type: docs"     --color 0075CA
gh label create "type: test"     --color FBCA04
gh label create "type: chore"    --color FEF2C0
gh label create "priority: high" --color D93F0B
gh label create "priority: mid"  --color FBCA04
gh label create "priority: low"  --color C5DEF5
gh label create "status: blocked" --color 5319E7
gh label create "good first issue" --color 7057FF
```

**담당:**


### 8.2 Project board

5개 컬럼 (`Backlog` / `Todo` / `In Progress` / `In Review` / `Done`) + 자동화.

**담당:**


### 8.3 동기화 시간

**후보:**
- 매일 짧은 standup (10분)
- 주 2회 중간 동기화
- 주 1회 회고

**결정:**


---

## 9. 시연 준비물

### 9.1 예시 기업 2~3개

대전·세종·충남 실재 기업 vs 가상 시나리오.

**후보:**

**채택:**


### 9.2 백업 시연 영상 녹화 여부

`[ ] Yes — 발표 1주 전(6/18)까지` `[ ] No — 로컬 시연만`


### 9.3 발표 PPT 작성 담당

**담당:**


---

# ⚪ 파킹 (시간 남으면)

- 라이선스 결정 (부트캠프 종료 후로 미룸)
- CD(자동 배포) 도입 시점
- Discussions 활성화 여부
- 로깅/모니터링 도구 도입
- 추가 ADR(스타일링, UI 라이브러리) 작성 시점

---

# 📌 Action Items (회의 종료 직후)

회의에서 결정된 항목을 다음 형태로 옮긴다.

- [ ] Issue 생성: **DB_SCHEMA.md 초안** — 담당: ___
- [ ] Issue 생성: **ARCHITECTURE.md 초안** — 담당: ___
- [ ] Issue 생성: **API.md 초안** — 담당: ___
- [ ] Issue 생성: **prisma schema 첫 모델 + migration** (위험 작업) — 담당: ___
- [ ] Issue 생성: **shadcn/ui 도입 결정 + ADR-0003** — 담당: ___
- [ ] Issue 생성: **SDGs 키워드 사전 + 시스템 프롬프트 초안** — 담당: ___
- [ ] Issue 생성: **Mock 응답 데이터셋** — 담당: ___
- [ ] Issue 생성: **Labels / Project board 생성** — 담당: ___
- [ ] collaborator 초대 + 솔로 정책 복원
- [ ] `.env.example`의 LLM 모델 ID 갱신 PR

> 발행한 Issue들에는 회의에서 합의한 **DoD(완료 조건)**를 명시한다.

---

# 📅 다음 회의

- **일시:** TBD
- **예정 안건:** 5/28 개발 착수 이후 첫 회고 / 블로커 공유

---

## 회의록 갱신 가이드

- 회의 중에 본 문서를 **라이브 편집**한다 (담당자: 기록자).
- 회의 종료 후 **회의록 PR**을 별도로 발행해 머지 — 결정사항을 기록으로 남긴다.
- 후속 ADR / 설계 문서 PR은 본 회의록을 참조한다.
